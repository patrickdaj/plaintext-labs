version: "3.9"

services:
  lab:
    build: .
    container_name: logforensics-lab
    volumes:
      - ./data:/data
      - ./scripts:/scripts
    working_dir: /lab
    entrypoint: ["sleep", "infinity"]
