# TODO: Write this Dockerfile as the lab exercise.
# Reference shape (do not use until you've written your own):
#
# FROM ubuntu:22.04
# RUN apt-get update && apt-get install -y --no-install-recommends curl ca-certificates git && \
#     rm -rf /var/lib/apt/lists/*
# # Verify the SHA256 checksum before installing
# RUN curl -sSLo /tmp/trufflehog.tar.gz \
#       https://github.com/trufflesecurity/trufflehog/releases/download/v3.88.1/trufflehog_3.88.1_linux_amd64.tar.gz && \
#     echo "<sha256sum>  /tmp/trufflehog.tar.gz" | sha256sum --check && \
#     tar -xzf /tmp/trufflehog.tar.gz -C /usr/local/bin/ trufflehog && \
#     rm /tmp/trufflehog.tar.gz
# RUN useradd -m scanner
# USER scanner
# ENTRYPOINT ["/usr/local/bin/trufflehog"]

# Placeholder that makes make demo fail with a clear message:
FROM ubuntu:22.04
RUN echo "Dockerfile not yet written — complete the lab exercise first." && exit 1
