services:
  target:
    build: ./target-app
    image: plaintext/web-target:1.0
    ports:
      - "5000:5000"

  lab:
    build: .
    image: plaintext/python-webscraper:1.0
    working_dir: /lab
    volumes:
      - ./:/lab
    depends_on:
      - target
    command: sleep infinity
